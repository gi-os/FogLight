<manifest xmlns:android="http://schemas.android.com/apk/res/android">
  <uses-permission android:name="android.permission.WRITE_SECURE_SETTINGS"
    tools:ignore="ProtectedPermissions"
    xmlns:tools="http://schemas.android.com/tools" />
  <!--
    Reading the name of the Wi-Fi you are on, which is how a privacy zone is recognised. A GPS fix
    indoors drifts or vanishes; a network name does not. Android also requires location permission
    before it will name a network at all, which this app already holds.
  -->
  <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

  <application>
    <service
      android:name="expo.modules.recorder.RecorderService"
      android:exported="false"
      android:foregroundServiceType="location" />
    <receiver
      android:name="expo.modules.recorder.BootReceiver"
      android:exported="false">
      <intent-filter>
        <action android:name="android.intent.action.BOOT_COMPLETED" />
      </intent-filter>
    </receiver>
    <!--
      Where you were, for the rest of the collection. Read-only, one row per stay, by day.
      Stays rather than fixes or tiles: a tile says which square of the world you crossed, and a
      journal wants the places you stopped. Privacy zones never reach the disk, so they cannot be
      served. See StaysProvider.
    -->
    <provider
      android:name="expo.modules.recorder.StaysProvider"
      android:authorities="com.gios.lightfog.stays"
      android:exported="true" />
    <!--
      Today's weather, for the rest of the collection — so nothing else ever needs a location
      permission just to print a temperature. Weather only: no coordinate is in any column, and
      the one behind the numbers was rounded to ~1 km before it left the phone. See
      WeatherProvider.
    -->
    <provider
      android:name="expo.modules.recorder.WeatherProvider"
      android:authorities="com.gios.lightfog.weather"
      android:exported="true" />
  </application>
</manifest>
